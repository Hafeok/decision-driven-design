Unzip migration-package.zip and read migration-package/README.md, then read
prompt-migration-exec.md in its entirety — this session follows that prompt exactly, including every
gate and all seven standing rulings.

This is the ground migration, executing. Its predecessor planned it in full and deferred it whole for
one stated reason: each of the 187 prose re-expressions is a ruling, and the first twenty would set
precedents the remaining 167 inherit unreviewed. This session exists to spend those rulings
deliberately — G2 is the whole answer to why the deferral happened, and if G2 fails to produce a
citable precedent set, the migration defers again.

First act, before anything else: commit this prompt and bootstrap to
meta/sessions/2026-08-31-ground-migration-exec/ in decision-driven-design, per DDD-dec-20.

Read meta/migration-plan-ground.md in decision-driven-design before anything else in the repos. It is
the seed: wave structure, every pin and firing named by id and hash, the four things not to
re-decide, and the method rule with its three mechanisms. The repo copy is authoritative over
anything in this package.

Fetch all three repos — actor-indexed-determination at v5.12.0 (verify and report the tag found),
decision-driven-design at head, product-cli read-only for W4 assessment.

Two things moved since the seed was written, both in the migration's favour: W1 is partly done
(Paper A's revision moved 15 of 31 in the two manuscripts, deferred 16 with reasons; 73 of 88
remain), and the measure note's pin advanced so the two papers agree on the moved sense.

Rules that override anything you might infer:
- Interactive execution. Stop at every gate for Emil's ruling. Merge nothing.
- The seven standing rulings are inherited and not re-litigated. SR-2 in particular: 380 of 567 S2/S3
  prose occurrences need no edit, because canon already names what is done to the ground.
- G2's twenty are selected for coverage, not convenience — every distinct sentence shape, hardest
  first. Every later re-expression cites which precedent it follows; one citing none is a new ruling
  and stops at a gate.
- The three method rules are what the predecessor learned by getting them wrong: clause-crossing
  windows are not anchored rules; report per-rule precision never one figure; classification data
  cites content, never position.
- State predicted W6/W7/W5 results before every pin operation, verify after, record divergence rather
  than reconciling it.
- W4 (product-cli's serialised ground: field, 660 occurrences) is assessed and reported at Gate 6,
  never executed here.
- Commit drafts before reporting at each gate, bodies marked draft-pending-ruling.

Begin with G1 and end your first report at GATE 1: the classification reconciled against head, which
rows the two merged sessions changed, any sense boundary that moves, and the corrected wave sizes.
