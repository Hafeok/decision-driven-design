# GATE 5 — i18n and the surfaces Gate 1 raised

**Status: draft-pending-ruling.** Upstream commit `5e75d23`; the map is
`g5-citation-map.py/.json`. Nothing merged.

## 1. The 54 surfaced bare rows — mapped, zero edits

**54 rows, 54 mapped 1:1 to the ratified precedents, zero orphans, zero edits.**
24 no-edit (P-01 ×2, P-02, P-03 ×3, P-06 ×4, P-07 ×3, P-08, P-12 ×2, P-15, P-16 ×3, P-17 ×2,
P-19 ×2) and **30 P-13 records** — the dated audits, holding notes (`ground-axes` above all, whose
sha256 is filed in `DDD-dec-20`), session records and the seed claims file. The unbooked surface
turned out to be mostly history plus a handful of sentences the precedents already cover; the
waves were right not to book it as work.

## 2. i18n — two translation defects surfaced by the gate's read, both repaired

The `grund` entry itself is correct since §6. Reading the whole file against the registry (the
gate's mandate: the file's own rule — *where Danish and English disagree, the English source
wins*) surfaced two glosses that disagree with settled canon:

| Entry | Defect | Repair |
|---|---|---|
| `lukker / lukning` (closure) | *"Om acceptprædikatet kan afgøres over **digital** grund"* — *digital* appears nowhere in `term:closure`, and *kan afgøres* imports the decidable reading canon explicitly reserves for the formal special case | re-glossed to canon's own conjuncts: observable relevant ground, adequacy evaluable within declared bounds, *afgørbar* reserved for the formal case |
| `forgiftet grund` (poisoned ground) | defined the term by **one generator** — self-consumed output — instead of canon's *present but false*; the closure principle's case standing in for the whole | first clause re-glossed to canon's definition; the second sentence (*"korrekte slutninger over falske præmisser"*) is a correct paraphrase and stays |

Same class as §6's double-gloss repair: the translation asserting what canon does not.

## 3. The 134 additions — the named-surface check

Per the Gate 1 ruling, the three `successor-items-*.md` files were read as the only candidates
for "a live surface rather than a record". **Every ground occurrence in them is a quotation, a
finding, or a programme mention — records. Nothing is named; they stay as written.** One is
flagged for the manifest: `successor-items-paper-a-revision.md`'s asset-divergence item says
`DDD-measure-12` and `term:verdict` *"still read `ground distribution` at v5.12.0"* — true of the
tag, and **discharged by G3 on this branch**; the item closes at the next pin advance.

## 4. Validators

Upstream after the i18n edits: 0 errors, 66 warnings (baseline); embeds match the graph.
Downstream untouched by this gate beyond the session record.

## What this gate asks

1. The 54-row map: 24 no-edit, 30 records, zero edits.
2. The two Danish repairs, as translation-defect repairs under the file's own rule.
3. The successor-items finding: nothing named as live; the asset-divergence item recorded as
   discharged-by-G3.

**Nothing merged. Holding at GATE 5.**
