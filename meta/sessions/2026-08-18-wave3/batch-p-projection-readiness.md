# Batch P — the projection-readiness map for Paper A

**Not a filing.** The map the Paper A session starts from, indexed by the revision foundation's
§12 manuscript structure. "Citable" means a node exists in canon at the Wave 3 branch (v5.7.0 on
acceptance); the front matter should pin the projection — *projection of
`actor-indexed-determination` at v5.7.0* — so the manuscript is reproducible against a tag. The
standing rule throughout: a paper may not introduce claims; register-native material is prose
argument, positioning, and worked examples only.

| §12 section | Citable from canon | Register-native or absent |
|---|---|---|
| 1. Introduction: the fixed arrangement | `DDD-frame-01` (the tuple, exposition core/14 §1 — "a parameter that never varies is indistinguishable from a constant" is now canon exposition); `DDD-dec-24` for the filing provenance | Motivation prose. **Q38b asymmetry thesis (software mechanised the determinable half and never the determination half; Pacioli anchor; LLMs' dual role): positioning prose available, deliberately unfiled** — travels with the paper; its survey burden (Codd, Chen, Evans, ADR literature) is the paper's, not canon's |
| 2. Actors, arrangements, ground | `term:actor`, `term:admission-test` (00 §4), `term:arrangement`, `term:act`; composite/ensemble: `term:composite-actor`, `term:ensemble-actor`, `term:swarm-gate`; **`term:determinable`/`term:determinate` (00 §4a, new)** for what alternatives *are* | The five-way ground-provenance taxonomy (§2.2: controlled/observed/inferred/institutional/missing) is **not canon** — institutional ground's mechanism is Q27-gated (rev18); project as manuscript analysis or wait for the post-Q27 wave |
| 3. Commitment resolution | `DDD-frame-02` (levels + residual discretion, core/14 §2) | Boundary-case walkthroughs |
| 4. Resolution and assurance | `DDD-frame-03` (two axes); `DDD-frame-04` (escaped decisions predict failures); `term:escape`; **the discharge chain: `DDD-frame-15`/`DDD-frame-16` — "demand is never unmet, only ungoverned" is canon, with the seam guard** (13 §4); seams: `term:seam`, `DDD-measure-03` | **§4.4's hedged allocation principle is superseded by canon**: the foundation avoided conservation "until an independently motivated measure exists" — it exists (09). Project `term:conservation` + `DDD-measure-01/02/06/10` + `DDD-frame-10/11` with the closing-region bound stated, not the hedge. §4.1's source table diverges from frame-15's modes (recorded in frame-15's notes) — project the claim, not the table |
| 5. Closure and evaluability | `term:closure` (operational closure is canon's closure; decidable the special case); `DDD-frame-06` (established — closure ≠ generation cost); `DDD-frame-05` (producer independence, with its "and nothing more" limits); `DDD-frame-11` (governed domain wider than measured); `DDD-cost-08/09/11` (assurance gates); `DDD-measure-11` (verdict, not search) | The four-way logical/operational/economic/normative closure taxonomy as a *named quadruple* is not canon — economic closure lives in the cost register, normative closure is unfiled; present as manuscript analysis or file later on demonstrated need |
| 6. Actor-indexed irreducibility | **`DDD-floor-02` (new — the relational floor, core/14 §3)**; `term:floor`; `DDD-floor-01` (capacity mechanism, *reported*, region-bounded per DDD-dec-15); `DDD-measure-06` (measure vanishes at the floor) | Brooks/Ashby/Tesler/Meyer reinterpretation prose |
| 7. Accountability completeness | `DDD-frame-08` (the relation); `term:accountability`/`attribution`/`answerability`/`liability` (05); `DDD-hyp-04` as the prediction | Ledger-limits prose (what provenance cannot supply) is 05-consistent exposition |
| 8. Worked example | Canon corroboration: 09 §3's computed example for the measure; the corpus results for register practice | The code-generation example itself — manuscript-native by design |
| 9. Predictions and study design | `DDD-hyp-01`–`05` + `DDD-frame-07` umbrella. **Carried as predictions with unrun falsifiers, never as findings** (GATE 4 ruling, verbatim requirement); evidence fields are empty by discipline and the paper must say so | The §9 study design (conditions, IVs, DVs, preregistered models) — the preregistration artifact is paper-4's, referenced not asserted |
| 10. Limits and boundary cases | The retirements as evidence the loop works: `DDD-frame-09` (retired), `DDD-measure-08` (retired); `term:escape-mechanism`'s scope note; `DDD-measure-15` (admissibility) | Hybrid/distributed boundary-case prose |
| 11. Related work | `DDD-frame-13`'s credits carry the verified determinable literature (Johnson 1921 Logic I ch. XI; Prior 1949 Mind 58(229):1–20, 58(230):178–194; Funkhouser 2014 OUP; Wilson SEP rev. 2023) | The full related-work section; **Q36 pull material (world-pull/ledger-pull, Goodhart as ledger-pull mis-aimed): positioning prose available, deliberately unfiled** — the ranking instrument stays corpus-gated with Q9 |
| 12. Conclusion | The relational statement is `DDD-floor-02`; the central sentence is `DDD-frame-15`+`16`'s compact form; `DDD-frame-14` for the registers | "DDD as the corpus the program was abstracted from" — positioning |

**The §14 editing instructions, checked against canon:** every "replace completely" item now has
its replacement in canon (species taxonomy → frame-01/02; "last wind" → residual discretion;
exact conservation → the bounded measure claims; intrinsic accountability → frame-08;
closed-predicate overreach → frame-05/06 with frame-09 retired). Every "preserve and adapt" item
survives with a canon cite. The training-gate softening in §8's H5 commentary is the one
sentence the paper may **not** carry as canon — it contradicts settled `term:training`; the
supersession question is queued, unbundled (successor item 1).
