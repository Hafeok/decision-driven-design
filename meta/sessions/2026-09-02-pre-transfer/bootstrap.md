# Bootstrap — the pre-transfer session

## Invocation message

> Read prompt-pre-transfer.md in its entirety — this session follows it exactly, including every
> gate.
>
> This is the last work before the repositories become public. Four items, chosen by one test: does
> this get more expensive, or become unfixable, once someone outside can read it or fork it? The
> accountability arity, which reaches canon; the README over-claim and the front pages generally;
> the pin advance to v5.13.0 with its two predicted firings; and a publication-readiness report.
>
> First act, before anything else: commit this prompt and bootstrap to
> meta/sessions/2026-09-02-pre-transfer/ in decision-driven-design, per DDD-dec-20.
>
> Fetch both repos at head — actor-indexed-determination at v5.13.0 (verify and report the tag
> found) and decision-driven-design at head.
>
> Rules that override anything you might infer:
>
> - Interactive canon curation. Stop at every gate for Emil's ruling. Merge nothing.
> - Supersession, never rewriting (DDD-dec-09, DDD-dec-10, DDD-dec-15).
> - I-1: report both texts verbatim before proposing anything, and propose three dispositions
>   rather than assuming one. A projected claim adding an element to a settled term is a
>   supersession question. Paper A's survey found four of the five elements are already Bovens's —
>   whatever files must not claim more than that.
> - I-2: report all findings before any repair, and repair over-claims rather than rewriting the
>   READMEs. A front page may say what the framework is for; it may not say what it has shown
>   unless a status carries it.
> - I-3: state the prediction before the operation, advance the ref first and re-instrument hashes
>   second so a pass is distinguishable from a skip, verify after, record divergence rather than
>   reconciling it. The primer regenerates at the new pin and its stamp updates.
> - I-4 is a report, not a repair pass. Emil rules what files now and what carries.
> - The test is publication, not tidiness. Untidy waits; unfixable-once-public lands now. Where the
>   two are hard to tell apart, report and let Emil rule.
> - Commit drafts before reporting at each gate, bodies marked draft-pending-ruling.
>
> Begin with I-1 and end your first report at GATE 1: DDD-frame-08 and term:accountability
> verbatim, every node depending on either, and the three dispositions with their costs.

## Parameters

- **Session type:** interactive canon curation; hold at every gate; merge nothing; supersession,
  never rewriting.
- **Branch (both repositories):** `claude/pre-transfer-publication-readiness-dh0dqo`
- **Base commits:**
  - `decision-driven-design`: `46d581a` (head of default branch at session start; the primer
    merged, PR #34)
  - `actor-indexed-determination`: `5c7fe46`, tag `v5.13.0` verified — the tag points exactly at
    the head of the default branch.
- **Gate count:** 5 (I-1 arity texts and dispositions; I-2 front-page findings; I-3 pin-advance
  prediction then verification; I-4 readiness report; close).
- **Principal:** Emil.
