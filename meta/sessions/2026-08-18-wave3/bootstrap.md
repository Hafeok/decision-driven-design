# Bootstrap — Wave 3: the principle filings (2026-08-18)

The invocation message that accompanied `prompt.md`, recorded verbatim as the session's arrival
record. Committed **before** any canon edit, per `DDD-dec-20`.

## Parameters

- **Branch:** `claude/wave3-principle-filings-edewf2` (both repositories)
- **Base commits:**
  - `actor-indexed-determination` — `01db7881a94b93292637d9e46863519d8a2083fc` (= tag `v5.6.0`)
  - `decision-driven-design` — `0452a40` (head; freight session merge)
  - `product-cli` — untouched
- **Gates:** 5 (survey · Batch Q · Batch W · Batch H · sweep/close)
- **Principal:** Emil
- **Session type:** interactive canon curation — hold at every gate, merge nothing

---

Read prompt-wave3.md in its entirety — this session follows it exactly, including every gate and
both evidence disciplines.

This is Wave 3: the principle-layer filings that make Paper A a projection. Four batches — W (the
indexed-determination core 3.1–3.4, surveyed before drafted because much may already be carried),
H (the five empirical hypotheses as a statement-grade set), Q (the derivation-grade chain:
determinable → outcome/verdict → supply-mode exhaustiveness, plus act-indexed discharge and
constitutive priority), and P (not a filing — the projection-readiness map Paper A starts from).

First act, before anything else: commit this prompt and bootstrap to
meta/sessions/2026-08-18-wave3/ in decision-driven-design, per DDD-dec-20.

Fetch both repos at head — actor-indexed-determination at v5.6.0, decision-driven-design at head.
Source material in the repos: the determinables addendum (upstream meta/), the ground-axes note
rev18 (downstream meta/holding-note-ground-axes-rev18.md), the corpus results, the Paper A
revision foundation (Emil uploads if not in-repo — hold at Gate 1 if absent rather than working
from summary; the arrival-failure discipline applies).

Rules that override anything you might infer:

* Interactive canon curation. Stop at every gate for Emil's ruling. Merge nothing.
* Supersession, never rewriting (DDD-dec-09/10/15). Upstream decisions cite upstream ground only
  (the freight session's going-forward convention).
* Survey before drafting in Batch W: refiling what exists is the failure mode; report
  carried/partial/absent with node IDs first.
* Every filing declares its evidence discipline — derivation-grade (premises by ID, steps stated)
  or statement-grade (falsifier strength, evidence debt named). Where a derivation check fails,
  the failure is the finding and files as a flagged hold, never a weakened claim.
* Q-A's seam guard is mandatory: governance-supply and discharge stay explicitly distinct, or the
  claim reads as contradicting term:store.
* Verify every bibliographic locator (Johnson, Prior, Funkhouser, Wilson) or flag unverified.
* Commit drafts before reporting at each gate, bodies marked draft-pending-ruling.

Begin with step 1 and end your first report at GATE 1: the W-batch survey with node IDs, the
H-batch overlap check, every Q-premise verified at head, proposed IDs and document homes.
