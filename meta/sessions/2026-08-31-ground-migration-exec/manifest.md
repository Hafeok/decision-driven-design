# Manifest — the ground migration, executed

**GATE 6: ACCEPTED (Emil, 2026-09-02). The migration closes.**

> **The ruling, recorded.** The close as assembled, `v5.13.0` as a minor with the descriptor as
> written, PRs upstream-first, the pin advance as a successor act — *it belongs to a session that
> can verify its own prediction rather than inheriting one.* **W4 closes as no-migration-owed**,
> the four local items riding `product-cli`'s own maintenance. And the observation Emil named as
> worth stating precisely: the expensive disposition — keeping the word where the world-facing
> sense lives — looked twice as costly when it was ruled, and turned out to **eliminate the only
> edit in the programme that could break something running**. The serialised field was already
> correct because canon kept its word.
>
> The closing summary, in the ruling's words: four instruments priced this migration in
> descending order and every reduction came from a ruling rather than a re-count. What executed
> was under twenty discretionary sentences across two repositories, 185 rows mapped to twenty
> precedents with zero orphans and a fallback count of one, and a translated defect nobody had a
> checker for. **The two-session shape is the finding: plan completely, defer whole, then spend
> the rulings first.**

**Session type:** interactive execution, six gates, Emil ruling at each. **Merged nothing.**
**Branch:** `claude/ground-migration-exec-teb4gc` in both repositories; `product-cli` read-only
throughout, no commit, no working-tree change.

**Read at:** `actor-indexed-determination` `81f6929` (= annotated tag **`v5.12.0`**, verified) ·
`decision-driven-design` `d89ed55` · `product-cli` `d0f4297` — the same commit the predecessor
read; that repository has not moved since before the audit.

---

## The arc, stated plainly

**The migration was deferred once, by a session that had planned it completely, and executed by a
session that spent twenty rulings first. The deferral was not a delay — it was the difference
between 167 unreviewed precedents and 131 rows each citing one.** That is the finding the whole
two-session arc exists to demonstrate.

And the second finding is its complement: **the migration is smaller than four instruments in a
row predicted** — the audit's ~2,357 migrating occurrences, the plan's 187 re-expressions, the
seed's wave sizes, Gate 1's 185-row bare ledger — and **each shrink came from a ruling, not a
re-count.** That clause is load-bearing and is not to be trimmed: it is the difference between a
migration that was over-scoped and one that was over-estimated. The work was always small, and
only the rulings could show it.

## What the session did, by gate

| Gate | Ruled |
|---|---|
| **G1** | The classification reconciled against head by content: 2,845 → 3,123 rows, 2,823 carried, every removal read and attributed. The 380/187 instrument was found never committed — rebuilt, frozen blind at 389/178, the ledger ruled governing and the figure recorded as a prediction that did not verify. **The fourth method mechanism filed in the seed.** 80 proposed senses; wave sizes corrected; W1's unbooked row booked |
| **G2** | **The twenty precedents, P-01…P-20, all ratified** — five edits, fifteen no-edit rulings. P-09's available/delivered boundary named as a precedent, not a wording; P-13 extended to notes recording decisions as formed, cross-referenced to `retired_from`; P-18 ratified as fallback with a counted condition; P-20 the cut trio's replacement, live on the public README |
| **G3** | The registry wave: **zero of the seed's seven W6 fire** — the definitional layer already correct; the seed's largest prediction, unverified and unmanufactured. Three unpinned entries move; three embeds re-project; W1's upstream body executed with its two W6 predicted by id and hash and landing character-exact. One forward-edge defect caught by the validator and repaired before commit |
| **G4** | The prose waves under the citation map: **131 rows, each citing exactly one precedent, zero orphans — 114 no-edit, 7 edits, 3 at G3, 6 records, 1 left by ruling** (the declared-carriage instance, recorded as precedent). P-18 fallback count: 1. The `encode-verify.md` miscitation repaired |
| **G5** | The 54 surfaced rows: mapped, zero edits — the waves vindicated by what surfaced outside them. Two Danish glosses repaired under the file's own rule. Nothing in the 134 meta additions named as live |
| **G6** | This close: `releases/v5.13.0.yaml` proposed; every instrument green; the basis-impact sweep clean; W4 assessed below, not executed |

## Predicted before operating, verified after

| | Predicted | Observed |
|---|---|---|
| the seed's seven W6 (registry) | 7 | **0 — recorded, not manufactured** |
| W6 at the next pin advance | exactly 2 — `DDD-measure-01` → `sha256:d78274d2…`, `DDD-measure-16` → `sha256:6828d066…` | **both landed character-exact; a sweep over all 71 pins shows exactly these two moved** |
| W5 / W7 / E12 / E13 | 0 / 0 / 0 / 0 | **0 / 0 / 0 / 0** |
| `DDD-frame-17` digest | unmoved by the falsifier edit | **unmoved** (`bf8d2c69…`); the region edit's hash was predicted (`03dc8ad7…`) and the edit ruled not taken |
| asset outputs | byte-identical | **byte-identical, verified by run** (three assets) |
| in-session downstream pin | untouched | 71 pins resolved, 0 basis-loss, 0 content-drift, 1 governed shadow |

## Instruments and gates

| | |
|---|---|
| `g1-reconcile.py`, `g1-pointA.py` | content matching over three corpus states; both matcher defects found, fixed, disclosed |
| `g1-bare.py` | the rebuilt qualified/bare instrument — anchored, clause-clipped, frozen after two warrant-disciplined revisions; per-rule precision reported (qualified 24/24, bare 9/12) |
| `g1-added-rulings.py`, `g1-head-ledger.json` | 80 proposed senses; the governing row ledger |
| `g4-citation-map.py`, `g5-citation-map.py` | 131 + 54 rows, each citing exactly one precedent, coverage asserted by run |
| upstream validators | 0 errors, 66 warnings (baseline, identical by content); 63 claims / 32 warnings; 12 decisions; **9 release descriptors valid** |
| downstream validators | 0 errors, 0 warnings; 26 claims / 6 warnings; 22 decisions |
| paper checkers @ `v5.12.0` | **30 verbatim, 0 failing · 96 status assertions, 0 wrong · 75 rendered, 75 cited, 0 discrepancies** |
| basis-impact sweep | no id moved; no orphaned old wording anywhere — the only survivors of changed sentences are quotes-as-history in the audit, the seed and the successor items |

## The carried items, dispositioned

| Item | Disposition |
|---|---|
| The seven standing rulings SR-1…SR-7 | Inherited, never re-litigated. SR-5's "fires no pins" superseded by `DDD-dec-34`'s post-seed pins — **a post-seed fact, not an error in SR-5** |
| The three method rules | Applied throughout; **a fourth filed** (an uncommitted instrument's figure is not reproducible) |
| The 380/187 figure | Recorded as a prediction that did not verify; the committed ledger governs |
| The seed's seven W6 / eleven embeds / four unpinned movers | 0 / 3 / 3 — divergences recorded at G3, accepted in advance |
| `encode-verify.md:21` | **Repaired** — the gloss owned by the apparatus, canon cited as canon states it |
| `poisoned ground` | One sentence, as the seed priced it — not 66 edits |
| `ground channel` ×10 | No edit, P-11/P-17; the surface known and standing |
| The 16/17 manuscript deferrals | Stand; they quote canon at the pinned tag and regenerate at the advance |
| The 134 meta additions | Out of scope by name (Gate 1); nothing named live at G5 |
| Printed strings + filename in `measure-nonuniform-ground.py` | **Booked as one item** for whoever next advances that asset, §5.4's disclosure clause the precedent |
| `DDD-frame-17`'s region | Left by ruling — **declared verbatim carriage inside a hashed field**, recorded as a named precedent instance |
| `successor-items-paper-a-revision`'s asset-divergence item | **Discharged by G3 on this branch**; closes at the next pin advance |
| Out of scope, untouched as chartered | W4's execution · the primer and the transfer · the `verdict` and `projection` collisions · the accountability arity · the measure note's missing checkers · Q40–Q46 |

## W4 — assessed, not executed

**Counts, verified exact at head:** 1,203 S-sensed mutable occurrences (S2 1,181 · S3 19 · S4 2 ·
S5 1); the serialised `ground:` field is **660 occurrences in 70 files** under `.ddd/`; the
repository is unmoved at `d0f4297`.

**The assessment's central finding: the executed migration removed W4's premise.** W4 was priced
as a data migration renaming the serialised field, with option B's "documented divergence" as the
cheap alternative. But the executed migration renamed no store and minted nothing — S1 keeps the
word, S2/S3 keep the word with delivery qualifiers, and only the population sense left.
`product-cli` is 98% S2, and **`ground:` therefore remains canon's own word for exactly the
object it serialises. Option A has no upstream driver left, and option B's divergence no longer
exists — the field agrees with canon as executed.**

What actually remains of W4, all local: the one S5 occurrence (a docs file quoting the caveat
that now reads *deployment distribution* upstream); the **`floor` collision** (`tolerance_floor`
against the Polanyi floor, 168 files) — real, local, and independent of any rename; the RDF
`ground term` (11) and the CSS surface colours (6), both for the drafting-warning instrument's
exception list; and the seven `mechanical` occurrences deferred from §6. **Recommendation: close
W4 as no-migration-owed, with those four local items riding `product-cli`'s own maintenance.**
Emil rules separately, with the running system in view, as chartered.

## The version proposal

**`v5.13.0`**, descriptor in the upstream branch (`releases/v5.13.0.yaml`, validated). A minor:
settled canonical text moves in three registry entries, four pinned claim statements move, the
population sense leaves the word. Not a patch for those reasons; not a major because every
format-1 claim remains valid unchanged, no id moves, no status moves. Merging the descriptor is
what cuts the tag; the summary was written here, in the session, not generated.

**The seam:** upstream lands and is accepted first; the downstream pin advance to `v5.13.0` is a
successor act, carrying the predicted two W6 above and filed as its own decision node in the
`DDD-dec-29` pattern.

## What is owed after this

The pin advance with its two predicted firings · the primer and the transfer, which were waiting
on this · W4's ruling on the assessment above · the freight-listed collisions and successor items
already on their lists, unchanged by this session.
