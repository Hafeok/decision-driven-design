format: 1
id: DDD-measure-04
kind: formal
statement: |
  Store allocation is actor-relative while total demand is actor-invariant: H(V) never mentions the actor; I(V;E) and H(V|E) split it per actor encoding.
status: reported
region: closing predicates; exercised on the date task, three actors
evidence:
- kind: asset
  ref: core/assets/measure-actor-allocation.py
  note: three actors, identical totals; reproduces exactly
falsifier: |
  An actor encoding on a closed task whose I(V;E) + H(V|E) departs from H(V), or a demonstration that the "constant across actors" reading collapses into per-actor difficulty.
breaks: the exact form of "fixed by the task, never by the system"
owner: paper-1
changed: v4.5
