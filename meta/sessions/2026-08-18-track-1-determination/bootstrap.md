# Bootstrap — Track 1: Determination, for software engineers

Invocation record and session parameters, committed with the prompt before Gate 1.

## Parameters

| | |
|---|---|
| **Date** | 2026-08-18 |
| **Type** | Projection authoring — one learning track, two repositories |
| **Principal / ratifier** | Emil (sole). This session proposes; it does not ratify. |
| **Upstream repo** | `Hafeok/actor-indexed-determination` |
| **Upstream branch** | `claude/track-1-determination-path-ugw29j` |
| **Upstream base** | `4d0d177` (`main`), tag `v5.7.0` |
| **Downstream repo** | `Hafeok/decision-driven-design` |
| **Downstream branch** | `claude/track-1-determination-path-ugw29j` |
| **Downstream base** | `8e348ce` (`main`), tag `v0.4.0` |
| **Pin at arrival** | `graph/upstream.yaml` → `ref: v5.7.0` |
| **Order** | Upstream PR first; the downstream pin is bumped on upstream acceptance |

## Gates

| Gate | Deliverable | Stops? |
|---|---|---|
| 0 | Repo state, tags, proposed file paths for both repos | yes |
| 1 | Upstream term deltas only | yes |
| 2 | Rungs 1–5 drafted | yes |
| 3 | Rungs 6–10, the falsifier, the linear-extension check | yes |
| 4 | Validators clean, PRs opened upstream-first | — |

## Standing constraints carried from the prompt

- Everything added is marked `[PROPOSED]`.
- The measure is not cited anywhere in the track — no `H(V)`, no entropy, no
  verdict-entropy. "Learn DDD" has no closing predicate; the measure is silent
  there and citing it would be the overreach the measure note's §6 exists to
  prevent.
- Rungs 1–9 supply **capability** only (`term:capability`); they do not establish
  competence. Stake starts at rung 10.
- The rung order is a declared, judgement-mediated decision with an owner. It is
  **not** derived from the canon dependency order; deriving it would be presumed
  discharge (`term:presumed-discharge`).
- Anything not confirmed by canon or by Emil is marked `UNVERIFIED — Emil review`.

## Divergences between the prompt's ground-state list and the live repositories

Recorded at arrival, per the convention that a session's charter is auditable
against what the session found. Each is reported at Gate 0, none resolved here.

| Prompt says | Live state |
|---|---|
| upstream `graph/axis-registry.yaml` | lives **downstream** at `graph/axis-registry.yaml`, `status: artefact-not-canon` |
| downstream `core/13-delivery.md` | lives **upstream**; downstream `core/13` is `13-cost-projection.md` |
| downstream `docs/g-track/` | does not exist; no `docs/` tree downstream |
| ground provenance as an upstream term delta | the five-way taxonomy is recorded **not canon**, institutional slot Q27-gated, and stands as a booked successor item |
