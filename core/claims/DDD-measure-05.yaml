format: 1
id: DDD-measure-05
kind: empirical
statement: |
  The identification survives an estimated channel: on a simulated RAG task, I(A;R) and H(A|R) estimated from samples sum to H(A) within estimator error across retrieval qualities.
status: reported
region: simulation only; a tractability result, not evidence of conservation
evidence:
- kind: asset
  ref: core/assets/measure-rag.py
  note: >-
    40k samples; answer generated independently of retrieval, so closer to construction than
    measurement — never present as conservation in the wild
falsifier: |
  Estimation on a real deployed retrieval system fails to recover the identity within error bars.
owner: paper-1
changed: v4.5
notes: |
  FLAGGED prose tension, not harmonised: core/09 §6.3 presents this as "conservation of specification demand, measured on a deployed system pattern rather than a toy", which overstates relative to this claim's region — the answer is generated independently of retrieval by construction, so the run is closer to construction than measurement. The claim keeps the honest form; the prose is the bug. Emil review.
