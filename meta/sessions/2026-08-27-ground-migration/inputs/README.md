# Item 5 — the ground migration

Upload this package to a Claude Code session with all three repositories available, and paste
`bootstrap.md` as the opening message.

## Contents

| File | Role |
|---|---|
| `prompt-ground-migration.md` | The session prompt. Six gates, seven standing rulings |
| `bootstrap.md` | Paste as the opening message |
| `ground-terminology-recommendation.md` | The original external proposal — source for the split |
| `ground-terminology-assessment.md` | Claude's assessment — where `basis` was excluded and the split ruled real |
| `q44-act-and-verdict-ontology.md` | Downstream demand item 1 — the term pair queued behind this ruling |
| `q45-routes-and-compositional-coverage.md` | Extends Q44; carried for context |
| `q46-encoding-price.md` | Carried for context; uses the vocabulary under review |
| `act-and-verdict-ontology-explainer.md` | Standalone explainer of the queued term pair |

**Not in the package, because they are in the repositories:** the ground audit
(`meta/ground-audit-2026-08-24.md`), its successor items, and the classification data from that
session. The repo copies are authoritative.

## What Emil rules

| Gate | Ruling |
|---|---|
| G1 | The completed classification, and any sense boundary that moved against the sample |
| G2 | Both sweeps — assert-not-define, and name collisions with their costs |
| G3 | The §7 / Q27 reconciliation |
| G4 | The migration plan and its size — execute, or defer whole |
| G5 | Execution checkpoints |
| G6 | Close, PRs, version |

Plus, at whichever gate they arise: whether `posterior` replaces the `mechanical` store, and whether
a `denominations:` field files for audience-scoped naming.

## The three things queued behind this ruling

Stated in the prompt so the session rules once with them visible:

1. **The act-ontology / verdict-ontology term pair** — Q44, Q45, and the explainer. Both will use
   whatever vocabulary this session settles.
2. **`posterior`** as a candidate for the `mechanical` store, if the collision confirms. A candidate,
   not a ruling — two prior naming attempts died on registry checks.
3. **Audience denomination** — canon as naming authority, projections denominating for their
   audience, with the denomination recorded on the term. Already canon's practice in one place
   (`term:verdict`'s parenthetical), never named as a rule. The primer is the first projection that
   needs it at scale.

## After this

The primer (item 6), then the transfer (item 7). Both were waiting on this.
